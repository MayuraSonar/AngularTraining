import { AssignInitalPipe } from './assign-inital.pipe';

describe('AssignInitalPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignInitalPipe();
    expect(pipe).toBeTruthy();
  });
});
